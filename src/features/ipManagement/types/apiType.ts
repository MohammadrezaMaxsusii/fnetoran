export interface API {
  id: string;
  device: string;
  ipAddress: string;
  requestedBy: string;
  action: string;
  attackType: string;
  evidence: string;
  createdAt: string;
  status: string;
}
